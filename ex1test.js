const soma = require("./ex1.js")

test ("Soma correta", () => {
    expect(soma(1,2)).toBe(3)
})

const sub = require("./ex1.js")

test ("Soma correta", () => {
    expect(sub(1,2)).toBe(1)
})

const mult = require("./ex1.js")

test ("Soma correta", () => {
    expect(mult(5,5)).toBe(25)
})

const div = require("./ex1.js")

test ("Soma correta", () => {
    expect(div(2,2)).toBe(1)
})